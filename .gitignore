# =======================================================
# 1. [기본 정책] 모든 파일을 무시한다 (Block All)
# =======================================================
*

# =======================================================
# 2. [필수 예외] 폴더 구조는 탐색 허용
# =======================================================
!*/

# =======================================================
# 3. [소스 코드] Verilog, C/C++, Linker, Assembly
# =======================================================
!*.v
!*.sv
!*.vh
!*.xdc
!*.tcl
!*.h
!*.c
!*.cpp
!*.s       # STM32 스타트업 파일 (어셈블리)
!*.ld      # STM32 링커 스크립트

!*.py       #python

# =======================================================
# 4. [문서/이미지] PDF, 문서, 그림 파일 허용
# =======================================================
!*.pdf
!*.txt
!*.md
!*.doc
!*.docx
!*.ppt
!*.pptx
!*.xls
!*.xlsx
!*.hwp
!*.png
!*.jpg
!*.jpeg
!*.gif
!*.bmp
!*.svg
!*.drawio

# =======================================================
# 5. [설정 파일] Git, STM32CubeIDE, Vivado 필수 설정
# =======================================================
!.gitignore
!README.md

# --- STM32CubeIDE / Eclipse Project Files ---
!*.ioc        # CubeMX 설정 파일 (가장 중요)
!*.project    # 프로젝트 정의 파일
!*.cproject   # C 빌드 설정 파일
!*.mxproject  # CubeMX 메타데이터
!*.launch     # 디버거 실행 설정 (선택사항, 협업 시 유용)
!*.xml        # Xilinx IP 공유시 필요한 파일

# =======================================================
# 6. [절대 무시] 허용된 확장자라도 이건 절대 안 됨 (이중 잠금)
# =======================================================
# Vivado 임시 폴더 및 파일
.Xil/
.runs/
.sim/
.ip_user_files/
.hw/
.cache/
*.log
*.jou
*.str
*.wdb
*.pb

# STM32CubeIDE 빌드 결과물 (절대 무시)
Debug/
Release/
*.elf
*.bin
*.hex
*.map
*.o
*.d
*.su